
import { festivalAverageRating } from 'crowd-sourced-review-website/helpers/festival-average-rating';
import { module, test } from 'qunit';

module('Unit | Helper | festival average rating');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = festivalAverageRating([42]);
  assert.ok(result);
});

